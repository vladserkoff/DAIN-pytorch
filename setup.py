# pylint: disable = missing-docstring
from setuptools import find_packages, setup

setup(name='dain-pytorch',
      version='0.1',
      description='Deep adaptive input normalization for PyTorch.',
      url='https://github.com/vladserkoff/DAIN-pytorch',
      packages=find_packages())
